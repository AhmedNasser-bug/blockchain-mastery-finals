---
---

<div class="card gc-card">
    <div class="gc-container">
        <button
            class="btn btn-secondary skip-btn"
            onclick="window.game.skipQuestion()"
            title="Skip Question (S)"
        >
            Skip <span class="hot-key">S</span>
        </button>
        <!-- Spacer removed for robustness -->
        <button
            class="btn btn-secondary hint-btn hidden"
            id="hintBtn"
            onclick="window.game.showHint()"
        >
            💡 Hint
        </button>
        <button
            class="btn btn-primary submit-btn"
            id="submitBtn"
            onclick="window.game.submitAnswer()"
            disabled
        >
            Submit <span class="hot-key">Enter</span>
        </button>
        <button
            class="btn btn-secondary next-btn hidden"
            id="nextBtn"
            onclick="window.game.nextQuestion()"
        >
            Next <span class="hot-key">→</span>
        </button>
    </div>
</div>

<style>
.gc-card {
    border-top: 1px solid var(--border);
    margin-top: auto;
    padding: 15px;
}

.gc-container {
    display: flex;
    justify-content: flex-end; /* Align items to right by default */
    align-items: center;
    width: 100%;
}

.skip-btn {
    margin-right: auto; /* Pushes everything else to the right */
}

.hint-btn {
    margin-right: 10px;
}

.next-btn {
    /* No default display:none here, handled by class */
}

.hidden {
    display: none !important;
}
</style>
